name: db-keepalive

on:
  schedule:
    - cron: "0 8 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /health (despierta Render y toca Supabase)
        run: |
          curl -fsS --max-time 120 --retry 6 --retry-delay 30 --retry-all-errors \
            "${{ secrets.KEEPALIVE_URL }}"
          echo
